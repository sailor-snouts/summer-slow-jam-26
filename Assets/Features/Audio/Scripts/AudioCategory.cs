namespace JamTemplate.Audio
{
    /// <summary>The categories audio is split across, each its own mixer group.</summary>
    public enum AudioCategory
    {
        Sfx,
        Music,
        Ambiance,
        Dialogue,
        Ui,
    }
}
